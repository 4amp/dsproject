# dsproject

### Основные выводы анализа данных

*   **Обзор и качество данных:** Набор данных содержит 287 записей об оружии и 17 столбцов. Несколько столбцов (`Damage`, `Damage Reduction`, `Aux Effects`, `Stat Requirements`, `Stat Bonuses`, `Stability`) изначально были определены как объекты строкового типа (`object`), требующие преобразования из-за содержания числовой информации, разделенной специальными символами.
*   **Обработка пропущенных значений:** Пропущенные значения были успешно обработаны: 'Sell Price' (1 запись) был заполнен медианой (200.0), 'Spell Buff' (254 записи) и 'Range' (270 записей) были заполнены нулями, а 'Stability' (14 записей) был заполнен медианой (30.0). В результате в этих столбцах отсутствуют пропущенные значения.
*   **Дублирующиеся строки:** Дубликатов в наборе данных не обнаружено.
*   **Ключевая числовая статистика:**
    *   Значения `Critical` показали низкую изменчивость, сосредоточенную вокруг 100.
    *   `Weight`, `Durability` и `Sell Price` демонстрировали более широкий диапазон и более высокие стандартные отклонения, что указывает на большее разнообразие среди оружия.
    *   Была обнаружена сильная положительная корреляция 0.811 между `Weight` и `Durability`, что предполагает, что более тяжелое оружие, как правило, более долговечно. Умеренные корреляции также были обнаружены между `Weight` и `Stability` (0.516), а также `Durability` и `Stability` (0.605).
    *   `Sell Price` показал слабые корреляции с другими числовыми признаками, что означает, что его стоимость менее прямо связана с физическими атрибутами, такими как вес или долговечность.
*   **Ключевая категориальная статистика:**
    *   "Normal Shields" (29 записей) и "Small Shields" (18 записей) были наиболее частыми категориями оружия.
    *   "Regular" был наиболее распространенным типом `Reinforcement` (153 записи).
    *   Приблизительно 58.9% (169 из 287) предметов были `Buffable`, а 49.1% (141 из 287) были `Infusable`.
